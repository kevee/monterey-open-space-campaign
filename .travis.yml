language: ruby
rvm:
- 2.1
node_js:
  - "4.4.7"

before_script:

before_script:
 - node --version
 - npm install grunt-cli -g
 - npm update -g
 - npm install

script: grunt setup && grunt deploy

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true # speeds up installation of html-proofer

sudo: false
