language: ruby
rvm:
- 2.1
node_js:
  - "0.10"

before_script:

before_script:
 - npm install grunt-cli -g
 - npm update -g

script: grunt; grunt deploy

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true # speeds up installation of html-proofer

sudo: false
